mixin alternatedHighlights(slice)
    ul#list
        each alternatedHighlights in slice.toArray()
            div alternated-highlights
                li !{alternatedHighlights.get("illustration").getView("main").url}